name: general_utilities
description: A set of general utilites usually needed for any project.
version: 0.0.4
homepage: https://github.com/nLoops/General-Utilities

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: ^0.18.0 # display SVG files

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

