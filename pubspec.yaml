name: general_utilities
description: A set of general utilites that required in any project pure in dart and cut the waste time of repetitive tasks.
version: 0.0.8
homepage: https://github.com/nLoops/General-Utilities

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  stack_trace: ^1.9.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

